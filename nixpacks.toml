[phases.setup]
nixPkgs = ["python39", "nodejs_22"]

[phases.frontend]
cmds = ["cd frontend && npm ci && npm run build"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
